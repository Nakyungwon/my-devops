apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  type: NodePort
  selector:
    app: web
  ports:
    - port: 80
      targetPort: 80
      # 고정 포트 — http://localhost:30080 으로 항상 접근 가능
      nodePort: 30080
